---
title: فهرس المرجع
lang: ar
section: "20.المرجع"
type: moc
tags: [reference, index, cheatsheet, moc, arabic]
updated: 2026-08-16
---

# 📇 فهرس المرجع

> طبقة البحث السريع. هذه ليست ملاحظات تُقرأ مرة واحدة، بل صفحات تُبقيها مفتوحة.
>
> 🇬🇧 النسخة الإنجليزية: [[EN/20.Reference/0.Reference Index|Reference Index]]

---

| الورقة | افتحها حين |
| --- | --- |
| [[1.رموز حالة HTTP]] | تُقرّر ماذا تُرجع، أو تُفكّك ما استلمت |
| [[2.مصفوفة حقول المُسلسِل]] | تحتاج نوع حقل أو خيارًا أو خطّاف تحقّق |
| [[3.ورقة ViewSet والـ Router]] | تربط العروض بالمسارات |
| [[4.كتاب طبخ الـ ORM]] | تعرف ما تريده بـ SQL ولا تعرفه بـ Django |
| [[5.أنماط الاختبار]] | تكتب اختبارًا وتريد الصيغة المعتادة |
| [[6.أوامر Docker و Compose]] | نسيت الأمر مرة أخرى |
| [[7.مرجع حقول النموذج]] | تُصمّم نموذجًا |
| [[8.المسرد]] | مرّ مصطلح وأومأت برأسك دون أن تعرفه |

---

## ⚡ الأوامر التي ستكتبها أكثر من غيرها

```bash
docker compose up
```

```bash
docker compose run --rm app sh -c "python manage.py test && flake8"
```

```bash
docker compose run --rm app sh -c "python manage.py makemigrations && python manage.py migrate"
```

```bash
docker compose run --rm app sh -c "python manage.py shell"
```

---

## 🎯 المقاطع الخمسة التي ستلصقها أكثر من غيرها

**حصر مجموعة الاستعلام على صاحب الطلب**
```python
def get_queryset(self):
    return self.queryset.filter(user=self.request.user).order_by("-id")
```

**ضبط المالك من التوكن لا من جسم الطلب**
```python
def perform_create(self, serializer):
    serializer.save(user=self.request.user)
```

**مُسلسِل مختلف لكل عملية**
```python
def get_serializer_class(self):
    if self.action == "list":
        return serializers.RecipeSerializer
    return serializers.RecipeDetailSerializer
```

**إخفاء كلمة المرور في الخرج**
```python
extra_kwargs = {"password": {"write_only": True, "min_length": 5}}
```

**قتل مشكلة N+1**
```python
.select_related("user").prefetch_related("tags", "ingredients")
```

---

## 🗺️ المخططات

![[drf-request-lifecycle.svg]]

![[api-endpoint-map.svg]]

---

## 🔗 روابط ذات صلة

- [[الصفحة الرئيسية]] · [[0.فهرس المفاهيم]]
- [[EN/19.Production DRF/0.Production Readiness Checklist|قائمة جاهزية الإنتاج]] 🇬🇧

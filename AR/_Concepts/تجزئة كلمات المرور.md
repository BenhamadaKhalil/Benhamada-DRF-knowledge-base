---
title: تجزئة كلمات المرور
lang: ar
type: concept
tags: [concept, django, security, passwords, auth, arabic]
updated: 2026-08-16
---

# 🔐 تجزئة كلمات المرور

> 🇬🇧 النسخة الإنجليزية: [[EN/_Concepts/Password Hashing|Password Hashing]]

---

## 🎯 Django لا يُخزّن كلمة المرور أبدًا

يُخزّن **تجزئة أحادية الاتجاه** — PBKDF2 مع SHA-256 ومِلح خاصّ بكل مستخدم:

```text
pbkdf2_sha256$1500000$KxvT2s...$9zN4bM...
    │             │        │        │
 الخوارزمية   التكرارات   المِلح   التجزئة
```

> [!INFO] Django 6.1 رفع عدد التكرارات إلى ١٬٥٠٠٬٠٠٠
> من ١٬٢٠٠٬٠٠٠ في 6.0 (و٣٩٠٬٠٠٠ في إصدارات 4.x). التجزئات القديمة تُرقّى تلقائيًا: عند أول تسجيل دخول ناجح يلاحظ Django أن العدد المُخزَّن أقلّ من الافتراضي الحالي فيُعيد التجزئة.

---

## ⚠️ السطر الذي يكسر كل شيء بصمت

```python
user.set_password(raw)       # ✅ يُجزّئ ويُخزّن
user.check_password(raw)     # ✅ يُجزّئ المُدخَل ويُقارن
user.password = raw          # ❌ يُخزّن نصًّا صريحًا. لا شيء يُحذّرك.
```

**لماذا هو خبيث؟** لأن نصف الوظيفة يبدو سليمًا. تُنشئ مستخدمًا، وتراه في لوحة الإدارة، ولا يخبرك شيء بأن كلمة المرور مكشوفة — حتى تفشل محاولة تسجيل الدخول الأولى، أو حتى تُسرَّب قاعدة بياناتك.

---

## 🧬 أين يحدث هذا في المشروع

```python
# app/core/models.py — UserManager
def create_user(self, email, password=None, **extra_fields):
    user = self.model(email=self.normalize_email(email), **extra_fields)
    user.set_password(password)
    user.save(using=self._db)
    return user
```

```python
# app/user/serializers.py — تجاوز سلوك ModelSerializer
def create(self, validated_data):
    return get_user_model().objects.create_user(**validated_data)


def update(self, instance, validated_data):
    password = validated_data.pop("password", None)
    user = super().update(instance, validated_data)

    if password:
        user.set_password(password)
        user.save()

    return user
```

> [!WARNING] لماذا تجاوز `update()` ضروري؟
> `ModelSerializer.update()` تفعل ببساطة `setattr(instance, attr, value)` لكل حقل — أي أنها ستُسنِد كلمة المرور الخام مباشرةً إلى العمود. هذا بالضبط الخطأ الصامت أعلاه، لكن من داخل الإطار.

---

## 🛡️ بقيّة القائمة

```python
extra_kwargs = {"password": {"write_only": True, "min_length": 10}}
```

```python
AUTH_PASSWORD_VALIDATORS = [
    {"NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator"},
    {"NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
     "OPTIONS": {"min_length": 10}},
    {"NAME": "django.contrib.auth.password_validation.CommonPasswordValidator"},
    {"NAME": "django.contrib.auth.password_validation.NumericPasswordValidator"},
]
```

`CommonPasswordValidator` يفحص قائمة مُضمَّنة بأشهر ٢٠٬٠٠٠ كلمة مرور — رخيص وفعّال.

**رسائل خطأ عامّة عند تسجيل الدخول:**

```python
"Unable to log in with provided credentials."     # ✅
"No user with that email."                        # ❌
```

الرسالة الثانية أداة **مجانية لعدّ الحسابات** — يستطيع المهاجم أن يعرف أيّ البُرُد مُسجَّلة عندك دون أن يخمّن كلمة مرور واحدة.

> [!TIP] سرّع اختباراتك عشرة أضعاف
> PBKDF2 بمليون ونصف تكرار بطيء **عن قصد**، وكل `create_user()` في كل اختبار يدفع الثمن.
> ```python
> # إعدادات الاختبار فقط
> PASSWORD_HASHERS = ["django.contrib.auth.hashers.MD5PasswordHasher"]
> ```
> **إعدادات الاختبار فقط.** لا تقترب بهذا من الإنتاج.

---

## ❌ التجزئة ليست تشفيرًا

لا يوجد «فكّ تجزئة». إن نسي المستخدم كلمة مروره فأنت تُصدر رمز إعادة تعيين، ولا تستطيع استرجاع الأصل.

> أي خدمة تُرسل إليك كلمة مرورك الحالية في بريد إلكتروني، تُخزّنها بشكل قابل للعكس.

---

## 🔗 روابط ذات صلة

- [[نموذج المستخدم المخصّص]] · [[مصادقة التوكن]]
- [[EN/19.Production DRF/10.Security Checklist|قائمة تدقيق الأمان]] 🇬🇧
